// Placeholder disabled sample tests for Lock (contract not in repo).
describe("Lock (disabled)", function () {
  it("is skipped because Lock.sol is absent", function () {
    // Intentionally empty
  });
});
